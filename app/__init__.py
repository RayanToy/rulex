"""RuLex: платформа оценки словарного запаса с LLM-генерацией заданий.

    api/       HTTP-слой: схемы запросов, зависимости, роутеры
    core/      база данных, модели, безопасность
    services/  предметная логика: тестирование, генерация, словари, модели
"""
from pathlib import Path

# Корень репозитория: data/, static/, templates/, migrations/, var/.
# Все пути строятся от него, а не от текущего каталога — раньше
# приложение работало, только если запускать его из корня репозитория.
PROJECT_ROOT = Path(__file__).resolve().parent.parent
